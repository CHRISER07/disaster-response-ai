# ARIA Disaster Response System — Python Dependencies
# Install: pip install -r requirements.txt

# --- LangChain ecosystem ---
langchain>=0.3.0
langchain-groq>=0.2.0
langchain-huggingface>=0.1.0
langchain-chroma>=0.1.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0   # FIX: was missing — used in core/knowledge_loader.py

# --- Agentic AI ---
# IMPORTANT: must be >=0.2.0 for state_modifier= kwarg in create_react_agent()
langgraph>=0.2.0
langgraph-checkpoint>=1.0.0        # Required for MemorySaver in LangGraph 0.2+

# --- Vector store ---
chromadb>=0.5.0

# --- Embeddings ---
sentence-transformers>=3.0.0
numpy>=1.26.0                      # FIX: was missing — required by sentence-transformers

# --- Dashboard ---
streamlit>=1.35.0
plotly>=5.20.0

# --- Data processing ---
pandas>=2.0.0
pypdf>=4.0.0

# --- Utilities ---
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# --- Data download ---
gdown>=5.1.0

# --- Testing ---
pytest>=8.0.0
